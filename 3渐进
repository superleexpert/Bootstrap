Bootstrap渐进
开发响应式页面
适配不同页面

实现栅格、图标、响应式

------
网页开发中的单位
px相对于显示屏幕分辨率的单位
em相对于当前文本的字体尺寸
rem与em类似，相对于HTML根节点的字体单位

px：
大小无法跟随屏幕放大缩小
所有浏览器都支持px单位

1em=16px（不完全是）
em会继承父集元素的字体大小
IE的部分浏览器并不支持em

rem会继承根元素的字体大小

------
Bootstrap依赖于Bootstrap.js
Bootstrap.js基于jQuery

